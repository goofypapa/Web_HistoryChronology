(function f(w) {
    w.goofypapaAjax = function (url , parmars , successCallback , failCallback) {
        if(typeof( goofypapaGame ) != "undefined" && goofypapaGame ){
            goofypapaAjax()
        }else{

        }
    }
})(window)